# =========================
# NEW GEN PATIO – .htaccess
# =========================

# -------------------------
# Seguridad básica
# -------------------------
<IfModule mod_headers.c>
  Header always set X-Content-Type-Options "nosniff"
  Header always set X-Frame-Options "SAMEORIGIN"
  Header always set Referrer-Policy "strict-origin-when-cross-origin"
  Header always set Permissions-Policy "camera=(), microphone=(), geolocation=()"
  # ⚠ Activar HSTS solo si TODO el sitio es HTTPS
  Header always set Strict-Transport-Security "max-age=31536000; includeSubDomains; preload"
</IfModule>

# -------------------------
# Canonical SEO: Forzar HTTPS + WWW
# -------------------------
<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteCond %{HTTPS} !=on [OR]
  RewriteCond %{HTTP_HOST} !^www\.newgenpatio\.com$ [NC]
  RewriteRule ^ https://www.newgenpatio.com%{REQUEST_URI} [R=301,L]
</IfModule>

# -------------------------
# Tipos MIME
# -------------------------
AddType image/avif                 avif
AddType image/webp                 webp
AddType font/woff2                 woff2
AddType application/javascript     mjs
AddType image/svg+xml              svg
AddType text/css                   css

# -------------------------
# Compresión Brotli/Gzip
# -------------------------
<IfModule mod_brotli.c>
  BrotliCompressionQuality 5
  AddOutputFilterByType BROTLI_COMPRESS text/css text/html text/plain text/javascript application/javascript application/json application/xml image/svg+xml
</IfModule>
<IfModule mod_deflate.c>
  AddOutputFilterByType DEFLATE text/css text/html text/plain text/javascript application/javascript application/json application/xml image/svg+xml
</IfModule>

# -------------------------
# Cache-Control (Lighthouse Optimization)
# -------------------------
<IfModule mod_headers.c>
  # HTML: nunca cachear
  <FilesMatch "\.(html?)$">
    Header set Cache-Control "no-cache, no-store, must-revalidate"
    Header set Pragma "no-cache"
    Header set Expires "0"
  </FilesMatch>

  # ✅ Archivos con HASH (Vite: nombre-HASH.ext) → cache 1 año + immutable
  # Coincide con: app-CIQE6ZDZ.js, index-AB12cdEF.css, foto-x9sd8f7G.webp
  <FilesMatch "-[0-9A-Za-z]{8,}\.(css|js|mjs|woff2|png|jpe?g|gif|webp|avif|svg|ico|mp4|webm)$">
    Header set Cache-Control "public, max-age=31536000, immutable"
  </FilesMatch>

  # Otros estáticos sin hash → cache moderado 30 días
  <FilesMatch "\.(css|js|mjs|woff2|png|jpe?g|gif|webp|avif|svg|ico|mp4|webm)$">
    Header set Cache-Control "public, max-age=2592000"
  </FilesMatch>

  # Ajustes adicionales
  Header unset ETag
  Header append Vary "Accept-Encoding"
</IfModule>
FileETag None

# -------------------------
# Evitar conflicto MultiViews
# -------------------------
Options -MultiViews
DirectoryIndex index.html

# -------------------------
# SPA fallback (React Router)
# -------------------------
<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /

  # Permitir acceso directo a assets reales
  RewriteCond %{REQUEST_URI} \.(css|js|mjs|map|json|png|jpe?g|gif|webp|avif|svg|ico|woff2?|ttf|eot|mp4|webm)$ [NC]
  RewriteCond %{REQUEST_FILENAME} -f
  RewriteRule ^ - [L]

  # Redirigir rutas desconocidas a index.html (SPA)
  RewriteCond %{REQUEST_METHOD} ^(GET|HEAD)$
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule ^ index.html [L]
</IfModule>
